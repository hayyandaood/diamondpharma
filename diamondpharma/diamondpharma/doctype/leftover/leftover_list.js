frappe.listview_settings['Leftover'] = {
    /*  before_render: (doc) => {
          // triggers before every render of list records
          console.log(doc);
          doc.used_qty = doc.data().used_qty;
      },
  */

}
